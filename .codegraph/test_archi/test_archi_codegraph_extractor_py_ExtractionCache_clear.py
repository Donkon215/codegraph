"""Architecture test for codegraph/extractor.py::ExtractionCache::clear."""
from codegraph.extractor import ExtractionCache

def test_archi_ExtractionCache_clear():
    obj = ExtractionCache()
    obj.clear()
